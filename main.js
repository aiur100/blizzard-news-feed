//TODO: We need to use npm to install fetch for node.
//fetch is a "library" that we will use to call data from the
//web. Google node-fetch. Look at the documents.

function getHtmlFromWebPage(webUrl)
{
    //TODO: returns the HTML string from
    //a given webUrl
}

function parseArticlOverviewDataHtml(webHtml)
{
  //IF article data is found, this should return
  //a list of objects.  Each object should have a
  // "title" and "url", field.
  //We will likely use regex
}

function parseFullArticleDataHtml(webHtml)
{
  //Parse out the full article 
}
